# 保证 deploy.sh 跨平台换行符安全，且 Git 不修改执行权限
deploy.sh -text

# 所有 shell 脚本统一使用 LF 换行符
*.sh text eol=lf

# 忽略二进制文件的文本处理
*.png binary
*.jpg binary
*.gif binary
*.zip binary
*.tar.gz binary

# 保留 .DS_Store 不跟踪（可选，但已经在 .gitignore 里）
.DS_Store export-ignore
